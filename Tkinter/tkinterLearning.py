import tkinter as tk

root = tk.Tk() #this creates the window
root.title("My First Window") #this sets the title of the window
root.geometry("400x300") #this sets the size of the window, width x height

root.mainloop() #this keeps the window open